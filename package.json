{
  "name": "wuzhu-blog",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vitepress dev src",
    "build": "git fetch --unshallow && vitepress build src",
    "cfbuild": "git fetch --unshallow && vitepress build src",
    "preview": "vitepress preview src"
  },
  "devDependencies": {
    "gitalk": "^1.8.0",
    "vitepress": "^1.0.2",
    "vue": "^3.4.21"
  }
}
